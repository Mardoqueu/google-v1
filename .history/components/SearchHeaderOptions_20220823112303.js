import { useRouter } from "next/router";


export default function SearchHeaderOption({ title, Icon, selected }) {
    return(
        <div>
            <SearchHeaderOption/>
            <SearchHeaderOption/>
        </div>
    )
}