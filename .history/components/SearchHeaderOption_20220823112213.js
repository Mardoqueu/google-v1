import { useRouter } from "next/router";


export default function SearchHeaderOptions({ title, Icon, selected }) {
    return(
        <div>
            <SearchHeaderOption/>
            <SearchHeaderOption/>

        </div>
    )
}